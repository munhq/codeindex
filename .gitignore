# Build artifacts
target/
zig/zig-out/
zig/.zig-cache/

# Vendored dependencies (fetched by script)
zig/vendor/

# Snapshots
.codeindex.json

# IDE
.idea/
.vscode/
*.swp

# Scratch that must never reach the public repo. Brand exports that nothing
# references live here; docs/brand/render.sh regenerates them on demand.
.local/

# Browser automation leftovers
.playwright-mcp/

# Local commit guard. Not committed: it is machine tooling, and the list it
# reads deliberately lives outside every repository.
.githooks/
